we can implements many of interfaces in a single class
(it was the hole that we can abuse interfaces to multiple inheritance )
but we should implement all of the methods in the interface
except the method is a default method in interface(we saw bad feature in java)
or our class is abstract(then the child should implement them)
-------------------------
we can implement a interface
in a multiple classes
its the main idea of interfaces
-------------------------
if interface A extends to interface B, then A includes all methods of B.
So if class C implements A, it must implement:
methods declared in A
methods inherited by A from B
we use this feature for "INTERFACE SEGREGATION"
-------------------------
a class just can implement interfaces,
a class can not extend to a interface
